## AI Tools Used
I used Claude to help me with this project.

Here are the prompts I used:

"What's the syntax for converting PIL Image to base64 in Pyodide?"

"Give me the type signature and return format for `pyodide.runPython()`"

"Do I need to manually revoke blob URLs created with `URL.createObjectURL()` or does React cleanup handle it?"

"Pillow Image.convert() grayscale mode - is it 'L' or 'LA'?"

"Base64 encode in Python - `b64encode()` returns bytes or string?"